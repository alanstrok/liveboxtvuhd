/** The name of the platform as referenced in the Homebridge config.json. */
export const PLATFORM_NAME = 'LiveboxTvUhd';

/** The npm package name; must match package.json. */
export const PLUGIN_NAME = 'homebridge-liveboxtvuhd';
